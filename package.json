{
  "name": "chrome-tabs-history",
  "version": "1.0.0",
  "description": "Chrome browser extension that keep an history of opened tabs and let the user move trought it",
  "main": "background.js",
  "scripts": {
    "test": "npm run compile && node test/*-test.js | ./node_modules/.bin/tap-spec",
    "compile": "browserify app/scripts/background-dev.js -o app/scripts/background.js"
  },
  "keywords": [
    "chrome",
    "browser",
    "tabs",
    "history"
  ],
  "author": "Edoardo Bianchi (bongofury)",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^8.1.0",
    "tap-spec": "^2.1.2",
    "tape": "^3.0.3"
  }
}
